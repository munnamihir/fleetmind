global:
  environment: production

api:
  replicas: 3
  autoscaling:
    enabled: true
    minReplicas: 3
    maxReplicas: 12

worker:
  replicas: 3
  autoscaling:
    enabled: true
    minReplicas: 3
    maxReplicas: 20

web:
  replicas: 3

assetWorker:
  enabled: true

archive:
  enabled: true

observability:
  otelEnabled: true
